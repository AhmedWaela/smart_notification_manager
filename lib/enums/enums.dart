enum NotificationType { local, push }

enum LinuxDefaultIcon { assets, byteData, theme, filePath }

enum LinuxDefaultSound { assets, theme }

enum TargetPlatform { android, iOS, macOS, linux, windows }